pluginManagement {
    plugins {
        id "com.github.johnrengelman.shadow" version "7.1.2"
        id "java"
    }
}

rootProject.name = 'EnterpriseDistributionAPI'
include 'api'
include 'proxy'
include 'backend'

